import {SidebarType} from "./store";

const initialState = [
    {id: 1, name: 'John'},
    {id: 2, name: 'Will'}
]

export const sidebarReducer = (state: SidebarType, action: any) => {

    return state
}
